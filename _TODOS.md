# _TODOS.md — Mafia Game Project Scaffold Summary

> Generated by project-manager skill. Every file contains only comments and TODOs — no executable code.

---

## Files Created (86 total)

### src/types/ (7 files) — Type Definitions
| File | Purpose |
|------|---------|
| `player.types.ts` | Role (19), Alignment, NightActionType (15), ImportanceTier, PlayerRole |
| `personality.types.ts` | PersonalityType (6), 17 stats, PerceptionDepth, VotingStyle |
| `game.types.ts` | Phase, SubPhase (9), Vote, BalanceScore, FullMoonState, WinResult, GameConfig |
| `chat.types.ts` | ChatEvent, ActionType (7), IndirectTarget, RoleClaim, Message |
| `memory.types.ts` | PlayerMemory, Relationship, KnownRole, EventWitnessed, NightResult, VoteRecord |
| `event.types.ts` | NightEchoEvent, NightEchoEventId (E01-E14), LastWish, FullMoonStage, PendingEvent |
| `role.types.ts` | RoleDefinition, NightActionConfig, TargetRule, SpecialRule, ZombieState |

### src/data/ (8 files) — Static JSON Data
| File | Purpose |
|------|---------|
| `roles.json` | 19 role definitions (keyed by RoleDefinition) |
| `personalities.json` | 6 personality types × 17 stats |
| `messageTemplates.json` | Phase 1 template pools (7 actions × 6 personalities × 3 intensities) |
| `nightEchoEvents.json` | E01-E14 event configs (probability, timing, linked_roles) |
| `lastWishActions.json` | 4 action types + probability weights |
| `fullMoonConfig.json` | Stage thresholds (0.05, 0.15), buffs, probability (0.15) |
| `gossipHints.json` | Gossip role cryptic clue templates |
| `config.json` | All tunable game constants (decay 0.85, timers, scaling) |

### src/state/ (5 files) — State Managers
| File | Purpose |
|------|---------|
| `GameState.ts` | game_state.json manager (phase, balance, Full Moon, zombie tracking) |
| `PlayerState.ts` | Per-player role + personality assignment |
| `MemoryManager.ts` | memory.json CRUD, time decay (r=0.85), zombie freeze |
| `ChatState.ts` | chat_events.json manager (ChatEvent[] + Message[]) |
| `EventState.ts` | Night Echo queue + Full Moon state + Last Wish state |

### src/engine/ (9 files) — Game Engine
| File | Purpose |
|------|---------|
| `PhaseManager.ts` | Phase transitions, sub-phase sequencing, timer control |
| `ResolutionEngine.ts` | 7-phase night resolution (phases 0-6) |
| `WinChecker.ts` | 6 win conditions in priority order |
| `NightEchoEngine.ts` | E01-E14 selection, weighted random, max 2/night |
| `FullMoonEngine.ts` | Balance check, stage determination, buff application |
| `LastWishEngine.ts` | 40% trigger on lynch, 4 action types |
| `ChatAnalyzer.ts` | Raw text → structured ChatEvent (7 action types) |
| `AIEngine.ts` | Orchestrator for all AI modules per player per sub-phase |
| `BalanceCalculator.ts` | balance_score formula for Full Moon staging |

### src/ai/ core (6 files) — AI Decision Layer (always if-else)
| File | Purpose |
|------|---------|
| `SpeakProbability.ts` | speak_chance = base × role × trigger × cooldown |
| `MessageGenerator.ts` | Decides WHAT to say, delegates HOW to AITextProvider |
| `VoteDecision.ts` | 8-step vote process + role overrides + personality modifiers |
| `NightDecision.ts` | Role-specific target selection for all 19 roles |
| `PerceptionFilter.ts` | 3-level filter (Superficial/Smart/Deep) limiting memory access |
| `EventReaction.ts` | Night Echo event → weight × emotional_reactivity → memory |

### src/ai/providers/ (5 files) — AI Text Layer (swappable)
| File | Purpose |
|------|---------|
| `AITextProvider.ts` | Interface (generateMessage, analyzeMessage, isAvailable) |
| `AIProviderFactory.ts` | Factory + FallbackProvider (Local → API → Template) |
| `TemplateProvider.ts` | Phase 1 — template pools from messageTemplates.json |
| `GemmaAPIProvider.ts` | Phase 2 — gemma-3-4b-it via cloud API |
| `GemmaLocalProvider.ts` | Phase 3 — gemma-3-1b-it via MediaPipe LLM Task |

### src/ai/prompts/ (3 files) — Prompt Engineering
| File | Purpose |
|------|---------|
| `messagePrompt.ts` | Prompt builder for message generation (full + compact) |
| `analysisPrompt.ts` | Prompt builder for chat analysis (JSON output schema) |
| `promptUtils.ts` | Context formatting helpers (relationships, truncation) |

### src/hooks/ (6 files) — React Hooks
| File | Purpose |
|------|---------|
| `useGameLoop.ts` | Main gameplay loop (Steps 1-5), phase transitions |
| `useChat.ts` | Public + Mafia chat, human input, silenced overlay |
| `useVoting.ts` | Mayor ×2, Zombie restriction, vote collection + tally |
| `useNightActions.ts` | Night action UI, AI action collection |
| `useMorningReport.ts` | Morning report assembly (deaths, events, Full Moon) |
| `useEvents.ts` | Night Echo + Full Moon + Last Wish lifecycle |

### src/utils/ (4 files) — Utility Functions
| File | Purpose |
|------|---------|
| `probability.ts` | rollProbability, weightedRandom, shuffleArray, clamp |
| `weightCalculator.ts` | Decay (r=0.85), indirect weights, combined score |
| `balanceScore.ts` | Ratio calculation, stage mapping for Full Moon |
| `formatters.ts` | Greek/English formatters (player, phase, role, time, death) |

### src/components/ (23 files) — UI Components
| Folder | Files | Purpose |
|--------|-------|---------|
| `chat/` | ChatBubble, ChatInput, PublicChat, MafiaChat, SilencedOverlay | Chat UI |
| `voting/` | VoteCard, VotePanel, VoteResult, LastWishBanner | Voting UI |
| `night/` | NightAction, MorningReport, ResolutionLog | Night Phase UI |
| `events/` | NightEchoBanner, FullMoonOverlay, ZombieIndicator | Event overlays |
| `shared/` | PlayerAvatar, RoleCard, Timer, MayorBadge, FactionBanner | Reusable UI |
| `lobby/` | PlayerCount, RolePreview, StartButton | Lobby screen UI |

### app/ (9 files) — Expo Router Screens
| File | Purpose |
|------|---------|
| `_layout.tsx` | Root layout (Stack navigator, fonts, splash) |
| `index.tsx` | Lobby screen (player count, role preview, start) |
| `settings.tsx` | Game settings (timers, AI provider, language) |
| `game/_layout.tsx` | Game layout (phase indicator, timer, day count) |
| `game/day.tsx` | Day Phase (Discussion + Mid-Day events) |
| `game/night.tsx` | Night Phase (actions + Mafia Chat) |
| `game/vote.tsx` | Trial & Vote (Mayor ×2, Zombie restriction) |
| `game/morning.tsx` | Morning Report (deaths, events, Full Moon) |
| `game/result.tsx` | Game Over (winner, role reveals, stats) |

### __tests__/ (11 files) — Test Stubs
| Folder | Files |
|--------|-------|
| `engine/` | ResolutionEngine, NightEchoEngine, FullMoonEngine, WinChecker, PhaseManager |
| `ai/` | VoteDecision, PerceptionFilter, SpeakProbability |
| `ai/providers/` | TemplateProvider, GemmaAPIProvider, AIProviderFactory |

### Root Config (4 files)
| File | Purpose |
|------|---------|
| `App.tsx` | Entry point (expo-router/entry export) |
| `app.json` | Expo configuration |
| `tsconfig.json` | TypeScript strict mode config |
| `package.json` | Dependencies + scripts |

---

## Implementation Order (Recommended)

### Phase 0 — Foundation
1. **Root configs** — `package.json`, `tsconfig.json`, `app.json`, `App.tsx`
2. **src/types/** — All 7 type files (zero dependencies)
3. **src/data/** — All 8 JSON data files (depends on types)

### Phase 1 — Core Engine (If-Else AI)
4. **src/utils/** — Utility functions
5. **src/state/** — State managers (depends on types)
6. **src/engine/** — Game engine modules (depends on state + types)
7. **src/ai/ core** — 6 Decision Layer modules (if-else logic)
8. **src/ai/providers/TemplateProvider.ts** — Phase 1 text generation
9. **src/ai/providers/AITextProvider.ts** + **AIProviderFactory.ts** — Provider infra
10. **src/ai/prompts/** — Prompt templates (used later in Phase 2)

### Phase 2 — UI Layer
11. **src/components/shared/** — Reusable UI components
12. **src/components/chat/** — Chat components
13. **src/components/voting/** — Voting components
14. **src/components/night/** — Night phase components
15. **src/components/events/** — Event overlays
16. **src/components/lobby/** — Lobby components
17. **src/hooks/** — React hooks connecting engine to UI
18. **app/** — Expo Router screens

### Phase 3 — Testing
19. **__tests__/engine/** — Engine tests
20. **__tests__/ai/** — AI decision tests
21. **__tests__/ai/providers/** — Provider tests

### Phase 4 — AI Migration
22. **src/ai/providers/GemmaAPIProvider.ts** — Phase 2 API integration
23. **src/ai/providers/GemmaLocalProvider.ts** — Phase 3 on-device

---

## Architecture Quick Reference

```
┌─────────────────────────────────────────────┐
│  APP LAYER (app/)                           │
│  Expo Router screens + components           │
├─────────────────────────────────────────────┤
│  HOOKS LAYER (src/hooks/)                   │
│  React hooks bridging UI ↔ Engine           │
├─────────────────────────────────────────────┤
│  ENGINE LAYER (src/engine/)                 │
│  Game logic, resolution, win check          │
├─────────────────────────────────────────────┤
│  AI LAYER (src/ai/)                         │
│  Decision (if-else) + Text (swappable)      │
├─────────────────────────────────────────────┤
│  STATE LAYER (src/state/)                   │
│  JSON file-based state management           │
├─────────────────────────────────────────────┤
│  DATA LAYER (src/data/)                     │
│  Static JSON configs + templates            │
├─────────────────────────────────────────────┤
│  TYPES (src/types/)                         │
│  TypeScript type definitions                │
└─────────────────────────────────────────────┘
```
