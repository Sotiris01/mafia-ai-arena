// =============================================================================
// FILE: App.tsx
// PURPOSE: Entry point — Expo Router bootstrap (renders app/_layout.tsx)
// LOCATION: App.tsx (project root)
// =============================================================================

// TODO(APPROACH): Minimal entry point. With Expo Router, this file simply
// re-exports the default from expo-router/entry. All routing is handled
// by the app/ directory structure.
//
// Collaborating files:
// - app/_layout.tsx                   — root layout (actual app structure)
// - app.json                         — Expo configuration

// TODO: Export default from "expo-router/entry"
// TODO(LOW): Add global error boundary wrapper if needed
